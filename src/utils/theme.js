import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#00c38c",
            contrastText: "#FFFFFF",
        },
        secondary: {
            main: "#8f98ff",
            contrastText: "#FFFFFF",
        },
    },
});
